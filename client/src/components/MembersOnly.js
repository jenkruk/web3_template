import React from 'react';
import { Button } from './styles/Button.styled'

const MembersOnly = () => {
  return (

    <Button
      style={{
          display: 'block',
          marginTop: '7px'
      }}
      >
          Members Only
    </Button>
  )
};

export default MembersOnly;
